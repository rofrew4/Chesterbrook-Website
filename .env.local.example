# Spline 3D scene URL for hero backdrop
NEXT_PUBLIC_HERO_SCENE=

# Looping video backdrops (mp4 URLs) — one per section
NEXT_PUBLIC_BACKDROP_1=
NEXT_PUBLIC_BACKDROP_2=
NEXT_PUBLIC_BACKDROP_3=
NEXT_PUBLIC_BACKDROP_4=
NEXT_PUBLIC_BACKDROP_5=

# Google Search Console → Ownership verification → HTML tag method
# Copy only the content= value (not the full meta tag), then add the same
# variable in Vercel: Project → Settings → Environment Variables
GOOGLE_SITE_VERIFICATION=
